// All the css helpers in one place

import classNamesFn from 'classnames';
import colorFn from 'color';
import { css as cssFn } from 'emotion';

export const css = cssFn;
export const color = colorFn;
export const classes = classNamesFn;
